name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    # Reusable release workflow: changelog + npm publish with provenance (OIDC).
    uses: sxzz/workflows/.github/workflows/release.yml@v1
    with:
      publish: true
    permissions:
      contents: write
      id-token: write
