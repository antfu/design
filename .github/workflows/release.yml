name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  release:
    # Reuses the shared release workflow (changelog + npm publish with provenance).
    uses: sxzz/workflows/.github/workflows/release.yml@main
